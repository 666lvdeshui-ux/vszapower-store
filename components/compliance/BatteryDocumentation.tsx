export const batteryModels = ['LIR2032','LIR2016','LIR2025','LIR2450','ML2032'] as const;
export default function BatteryDocumentation(){return <div id="battery-documentation"><p>Request documentation for LIR2032, LIR2016, LIR2025, LIR2450 and ML2032 rechargeable coin cells. Confirm capacity and product version so that documents are matched to the correct tested sample.</p><div className="battery-documentation-grid">{batteryModels.map(model=><article className="evidence-card" key={model}><h3>{model}</h3><p>{model==='ML2032'?'Provide the ML2032 cell specification and charging requirements. An LIR charging profile should not be assumed suitable.':'Provide nominal voltage, capacity and the exact product version. Matching dimensions alone do not identify the tested battery.'}</p><a href={`#request-${model.toLowerCase()}`}>Request {model} documents →</a></article>)}</div>{batteryModels.map(model=><details className="evidence-faq" id={`request-${model.toLowerCase()}`} key={model}><summary>{model} documentation checklist</summary><p>Include {model}, nominal voltage, capacity, product version, destination market and document purpose in the enquiry form below. Specify whether you need sample test documentation, material test information or documents for a particular shipment. Availability and scope must be confirmed for the selected version.</p><a href="#document-request">Open document request form →</a></details>)}</div>;}
